import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // This test simply checks if the app can boot up
    // without hitting a fatal error.
    expect(true, true);
  });
}
